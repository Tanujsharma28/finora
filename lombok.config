# Lombok configuration — suppress IDE yellow warnings
# Stop Lombok from searching parent directories
config.stopBubbling = true

# Suppress "field is never assigned" warning for @Builder fields in JPA entities
lombok.addLombokGeneratedAnnotation = true

# Suppress warnings about @Builder with @NoArgsConstructor/@AllArgsConstructor
lombok.anyConstructor.addConstructorProperties = true
